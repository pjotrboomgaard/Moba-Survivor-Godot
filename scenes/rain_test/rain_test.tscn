[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/rain_test.gd" id="1_test"]

[node name="RainTest" type="Node2D"]
script = ExtResource("1_test")

[node name="Camera2D" type="Camera2D" parent="."]
position = Vector2(0, 0)
; T3.78: match the game's default zoom (0.5) so the rain full-screen fix is
; exercised at the zoom level where the old * zoom bug shrank the rain area.
zoom = Vector2(0.5, 0.5)
position_smoothing_enabled = false
